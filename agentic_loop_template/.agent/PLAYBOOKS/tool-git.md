# Playbook: tool-git
Scope: tool:git

- [0.92] Для кросс-клона используй git -C /path ... + явный маркер SYNC_DONE / VerifyOnly.  (tags: ['sync', 'verification'])
- [1.00] playbook git + performance delta wins  (tags: ['auto-from-reflection'])