# Tests

```bash
# with venv
./.venv/bin/python -m pytest -q
# or
python -m pytest -q memory/
```

Always use project venv when it exists. Capture `tests_total` / `tests_failed` into handoff `metrics`.
